<div class="lpe-section-menu">
  <h4 class="visually-hidden">{{ 'Add Section'|t }}</h4>
  <div class="lpe-section-menu__group">
    {% for type in types %}
      <div class="lpe-section-menu__item type-{{type.id}} is-layout">
        <a class="lpe-section-menu-button" data-type="{{ type.id }}" href="#{{ type.id }}" title="{{ type.title }}">
        {% if type.image %}
        <img src="{{ type.image }}" alt ="" />
        {% endif %}
        <div>{{ type.title }}</div>
        </a>
      </div>
    {% endfor %}
  </div>
</div>